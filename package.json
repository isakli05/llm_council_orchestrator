{
  "name": "llm_council_orchestrator",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "_archival": "ARCH-001 (residual closure, 2026-08-27): the archived legacy tree (apps/*, packages/shared-*, scripts, monitoring, .kiro, .audit, tests, test-output, plans-out, tasks, architect.config.*, root tsconfig/vitest) was DELETED from the active workspace — see docs/legacy-archive.md and tag legacy-archive-final. The workspace is packages/spec-core only. The root offers one convenience alias (test:spec); CI and docs use the PATH-filtered form directly.",
  "scripts": {
    "test:spec": "pnpm --filter ./packages/spec-core test"
  }
}
