{
  "name": "llm_council_orchestrator",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "_arch_note": "ARCH-001: legacy root targets removed 2026-08. apps/* and packages/shared-* are archived, broken by design, and deliberately not runnable from the root. The root offers one convenience alias; CI and docs use the PATH-filtered form directly (see README 'Legacy (archived) — do not run').",
    "test:spec": "pnpm --filter ./packages/spec-core test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^2.1.0",
    "axios": "^1.13.6",
    "fast-check": "^3.22.0",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^2.1.0"
  }
}