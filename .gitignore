node_modules/
dist/
.DS_Store
.env
.env.local
.env.test
vscode-extension.vsix

# Prevent compiled JS files in source directories
# Only TypeScript source files should exist in src/
apps/*/src/**/*.js
packages/*/src/**/*.js
!apps/*/src/**/*.test.js
!apps/*/src/**/*.spec.js

# Graphify: keep the persistent graph, report, HTML, manifest, and integration
# files versionable; keep query/runtime caches and dated safety backups local.
graphify-out/cache/
graphify-out/cost.json
graphify-out/20??-??-??/
