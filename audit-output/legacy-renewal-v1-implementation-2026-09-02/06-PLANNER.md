# 06 — Planner (STEP 10)

`src/renew/planner/strategy.ts`: strategies as data (6), `selected_by:'human'` a schema invariant; selection via workspace (approval lineage) or explicit flag (rationale REQUIRED); `.lco/renewal/strategy.json`. `plan.ts` `buildModernizationPlan` — pure, zero LLM: gates (empty ledger / unresolved parity with actionable blockers / missing strategy — staleness gated by the command core), then a SpecBundle on the existing spine (mode `legacy`, profile `p-legacy`, NFR budget requirement for L07, strategy as accepted DEC with the five rejected alternatives, code_anchor evidence items mapped from parity anchors + user_input strategy evidence, requirements per parity entry with TST acceptance refs, migration tasks on TaskContract: permitted_scope=anchor paths, protected=preserves, depends_on from injected Graphify blast radius — impacted files migrate AFTER their dependency; same-file scopes chained for L12; cycles = honest blockers suggesting merge — REAL verification (`lco compile/verify <dir>` expect `exit 0`), tests ledger anchored to `.lco/renewal/parity.json`, deterministic Kahn topo order). Bundle passes SpecBundleSchema + lintBundle (planner bug if not) and is written via the EXISTING `writeSpecDir`; `--freeze` runs the existing freeze gate → immutable revision. Execution: NONE — the plan describes future work; no source edits, patches, worktrees, or shell against the target exist anywhere in the renewal code.
