# 05 — Clarification & Parity (STEPS 8–9)

Clarification: the audit's three generalization points, minimally invasive — (a) question SOURCE: `src/renew/clarify/distiller.ts` (promoted uncertainties; overlay manual_review/uncertain_behavior; PAR-questions for unresolved unlinked parity entries; the always-present human strategy question over the six modeled strategies); (b) ids: claim-id pattern seam in `clarify/model.ts` `validateAnswer`/`applyAnswersToRecords` (default `DEC-\d{4}` — spec flows byte-identical, existing suite green); renewal ids `UNC|OVL|STG|PAR-NNNN`; (c) approval schema: `RenewalApprovalRecord` (APPR-NNNN, 0600, write-once, content digest, canonical locally-hashed user evidence per decision). `createRenewalClarifySession` implements `ClarifySession`; the loopback server, token-in-fragment, state machine, browser client, and records model are reused UNCHANGED (server is duck-typed). Rounds are deterministic (no LLM): answered→revalidate by recomputation; resurfaced→contradicted semantics from the shared model; approval guarded (zero pending, zero open/stale/contradicted); write failures surface, never silent. Change sets refused (no review document in renewal). Real-server round-trip test: question → answers → approval → immutable record; unauthenticated 401. CLI: `--interactive` (server + browser) or headless `--answers`.

Parity (`src/renew/parity/ledger.ts`): `preserve_change_drop` promoted — entries `{behavior, ruling preserve|change|drop|unresolved, rationale?, evidence[anchors|user_decision], decision_claim_id?, approval_id?, source_analysis?}`. Invariants (schema + gate): non-unresolved REQUIRES rationale; DROP REQUIRES approval lineage; empty ledger refuses planning (run analyze). Seeding from analyses: every promoted hypothesis enters UNRESOLVED (no silent drop). Approval folding maps canonical preserve/keep/retain & drop/remove language; ambiguous approved text stays UNRESOLVED with the text recorded (visible blocker) — explicitly resolvable via the PAR questions or a recorded explicit ruling. Gate re-verifies anchors against the live tree (stale anchors block). Projection to the spec legacy package refuses partial ledgers; atomic persistence, fail-closed reload.
