# 02 — Code Intelligence (STEP 1)

`src/renew/intel/`: `provider.ts` (interface: probe/build/graph/query/path/explain/affected/godNodes/graphHealth; fail-closed result types), `subprocess.ts` (explicit argv, no shell + executable charset guard, timeouts, per-stream caps without buffering past them, stderr preserved), `graph-reader.ts` (defensive node-link parser; additive-field tolerant; dangling links dropped+warned), `graphify-adapter.ts` (version pin `>=0.9.50 <0.10.0`; actionable not_installed/unsupported/probe_failed/build_failed/graph_missing/graph_invalid; build = `graphify update <LCO workspace>` only), `graph-ops.ts` (god nodes symbol-level — cross-checked vs CLI on fixture; reverse-impact `affected`; deterministic BFS path; label-token query), `fixture-provider.ts` (committed graph for offline tests; materializes `graphify-out/graph.json` in the workspace like the real adapter). Interface additions during build: `graph()` (expose parsed graph for context/archview) and `build({workspaceRoot})` — both implemented by both providers. Note: audit's `graphifyy` spelling is a typo; binary is `graphify` (verified live).
