# 07 — CLI & MCP (STEP 11)

CLI grammar in `src/cli/args.ts` (`renew <sub> <dir>` + flags; `--help` per command; USAGE lists the family with the PAID marker on analyze). Cores in `src/cli/commands/renew.ts` (pure; `RenewCapabilities` injection: nowIso/provider/gitCommit/llm?/budget?/openBrowser?). Boundary `cli/index.ts`: constructs `GraphifyAdapter(workspaceRoot=.lco/renewal/graph-workspace)`, `git rev-parse` with timeout, LLM resolution = named profile with an explicit `renew_recover` route (refused otherwise, zero calls) or legacy `LCO_LLM_*` env (fail-closed); browser opener detached. Command classes: init/refresh/status/export offline; analyze PAID (refuses stale snapshots with reasons + refresh remedy; zero calls without a route); review clarification; plan offline deterministic (refuses stale/unresolved; optional explicit strategy; optional --freeze); export renders validated state only (deterministic markdown). Renewal project state module `src/renew/project/{project,export}.ts` (atomic JSON stores; status aggregation). Doctor: informational `graphify` check (warn when absent — non-renewal usage unaffected; tests updated to the pinned 11-check contract).

MCP: `lco_renew_status`/`lco_renew_export` read-only ungated (containment via existing `checkMcpDir` machinery); `lco_renew_analyze` PAID — `LCO_MCP_ALLOW_GENERATE=1` + `consent.digest` = `renewConsentDigest(dir, scope, llmProfile?)` (payload shape distinct from generate/check digests — no cross-tool replay); refusals advertise the digest and precede any adapter (ZERO LLM calls — tested for missing consent, wrong digest, and not-opted-in). Graphify MCP is never proxied; no renewal tool executes anything. Stdio discipline unchanged. Packed-install smoke now also proves `renew` help + status fail-closed offline (no Graphify needed for non-renewal use).
