# 04 — Recovery & Overlay (STEPS 6–7)

Recovery (`src/renew/recovery/`): gated stage on the SAME `LlmPlan`/`LlmAdapter`/`BudgetLedger` machinery (role `renew_recover`; no second transport stack): complete → stripJsonFences → zod (`RecoveryOutputSchema`: BHV-NNNN hypotheses {category∈7, confidence, ≥1 anchor, rationale}; UNC-NNNN uncertainties {≥2 options, anchors}; coverage notes; **no status field — the model never labels trust**) → ONE validation-informed retry → blocked record persisted on double failure. Then INDEPENDENT anchor verification against the live target: all-verify → `hypothesized`; any failure → `rejected[]` with per-anchor reasons (hallucinated files and stale hashes never promote; uncertainties held to the same bar). Records immutable (`wx`, 0600): model identity, prompt protocol `lco-renew/recovery-v1`, context digest + counts (never full prompts), honest usage (unknown ≠ 0), budget/transport errors propagate with zero writes. Prompts fence ALL repo content (markers never spelled in the rules text — canary proven single-occurrence inside the fence), declare no-tools/no-execution/no-write, and expose only the anchorable-files hash table (slice items carry the whole-file hash). Uncertainty→parity linking at fold time is deterministic (anchor overlap).

Overlay (`src/renew/overlay/`): `overlay.json` v1 — records over the 13-relation audit vocabulary, each `{subject{node_id?,path,symbol?}, value?, anchors≥1, snapshot_id, confidence, status, lineage{analysis|decision|approval}, note}`; strict schemas; atomic staged persist; stable id order (diffable); derived staleness (active⟷stale follows CURRENT verification — a revert honestly reactivates; `superseded` terminal). Graphify's graph is never modified; overlay is LCO-owned data.
