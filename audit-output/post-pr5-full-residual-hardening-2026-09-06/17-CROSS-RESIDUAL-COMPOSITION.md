# 17 — Cross-residual composition (R1–R4)

Committed suite: `src/renew/trust/post-pr5-composition.test.ts` (5 tests,
green) + the deep matrix cells in `transaction-atomicity.test.ts` (L5/L6/L7/I5
describes) and `composition.test.ts` (Composition H, S5-M-01×M-02×M-04).

## R1 — canonical identity × consent (L1+L2+I2+I4)

One cell composes: env `__proto__` extraBody refused loudly (no route/consent/
wire); zod-bypassed own-`__proto__` header is digest-BOUND (clean-config
consent ≠ poisoned-route consent; the total gate refuses the crossed pair);
an unresolved binding can never authorize a constructed operation; a getter
item cannot split the seal (identity covers the exposed frozen clone; the
citation resolves against those bytes). RESULT: no divergence constructible.

## R2 — ContextBundle identity × framing × consent authority (L3/L4)

Framing changes move the model-visible prompt but never `bundle_id`; scope
changes move the consent digest but never the bundle; the exact v2 preimage
shape reconstructs byte-identically (pinned against field leaks). RESULT: the
three identity domains are mechanically distinct.

## R3 — paid operation × recovery (L2+L5+L6)

A header-bearing consented route joins its consent exactly (gate returns
proceed); a post-consent header mutation is refused (`no longer matches`,
zero transports). Deep abort/evidence/recovery cells live in the
transaction-atomicity matrix (Composition H2 covers the transport-failure
abort; L5/L6 cells cover the abort evidence arms). RESULT: no unauthorized
call, no rollback-authority regression.

## R4 — storage degradation × foreign object × fresh process (L5+L6+I5)

- A FOREIGN object at the evidence path fail-closes a GENUINE separate
  process (execFileSync child requiring dist state.js) with the
  distinguishable foreign refusal; removing the object restores the healthy
  read — the fail-close is about the object, not permanent corruption.
- The L6 boundary cell pins: total evidence-channel failure → truthful
  disclosure, NO retention claim, fresh reader HEALTHY at R+1 (module-fresh
  + separate-process when dist present); contrast cell: a LANDED sidecar
  fail-closes reads.
- The entry health probe leaves no authority residue in the renewal root.

RESULT: the claimed boundary is exactly the pinned one.
