# Plan — Pre-v0.2.1 Final Residual Hardening (8 Low + 19 Info)

Date: 2026-09-06. Orchestration: MAO adaptive mode (skill loaded; primary agent = PM/orchestrator + sequential trust-surface implementer + final integration authority).

## 1. Baseline (verified)

```
origin/main        = 1b7fe6e3a4a7fc30bbbe5f23cfa1482ba901a7ea
origin/main tree   = be5fe190addf64c602a9496b3b6a5120d9f3e67c
v0.2.0             = e7dedf034e92fc57616124dbdd7fe6ebffda8620  (immutable; untouched)
audited ancestor   = 0063ce37d9154352bbaf2bd7c7f87fbe3d4436f5 (runtime, subtree-identical on packages/)
branch             = fix/pre-v0.2.1-final-low-info-hardening (from origin/main)
```

Phase-0 preflight passed; no tracked modifications; only untracked audit artifacts.

## 2. Canonical ledger

`audit-output/pre-v0.2.1-final-low-info-hardening-2026-09-06/00-CANONICAL-RESIDUAL-LEDGER.md`
— 8 Low (E-1, E-2, E-3, F-L6-1, NF-1, NF-2, D-F-01, H-1) + 19 Info (F-I5-1/2, F-L7-1, NF-3/4, B-1/2, C-1/2, D-OBS-1..4, G-1/2, H-2/3, M-1/2) = 27 unique. Authority: re-audit dir `post-pr5-full-residual-targeted-independent-reaudit-2026-09-06/` (reports 16/23 primary). NF-1/NF-2 severity discrepancy resolved in favor of re-audit (Low).

## 3. Finding-to-source map (from re-audit lane reports)

| Finding | Primary source surface | Test surface |
|---|---|---|
| E-1, E-2, E-3 | `packages/spec-core/src/renew/trust/state.ts` (`markJournalSuperseded`/`removeJournal`, EEXIST corner ~:784-818 at audit) | `test/renew/transaction-atomicity.test.ts` (+ fresh cells) |
| F-L6-1 | `packages/spec-core/src/cli/commands/renew.ts` (probe placement) | new pin test (renew.test.ts / e2e) |
| NF-1 | `packages/spec-core/src/config/llm-config.ts` (roles record schema) | `llm-config.test.ts` |
| NF-2 | `llm-config.ts` `resolveProfile` (~:255,:285); callers MCP `server.ts:1262`, CLI `index.ts:357,563` | `llm-config.test.ts` |
| D-F-01 | `state.ts` `applyStateMutation` serialization boundary | `transaction-atomicity.test.ts` / store tests |
| H-1 | `packages/spec-core/test/browser/app.test.ts` (12 `settle()` sites) | same file (self-hardening) |
| 19 Info | per ledger; mostly documentary/bounded; actionable subset decided in Phase 2 | varies |

Exact line numbers re-derived on current main during Phase 2 (audit cited positions at e47c448; main adds only docs commit content, so lines should match — verify anyway).

## 4. MAO topology

- **Primary (me):** ledger/plan (done), shared contracts, ALL production edits (trust surfaces overlap heavily: `state.ts` carries E-1/E-2/E-3/D-F-01; sequential mandatory), integration, mutation adjudication, final validation, reports.
- **Wave 1 — parallel read-only investigators (Explore agents, no spawn, no writes):**
  - INV-A: config/canonicalization — NF-1, NF-2, NF-3, NF-4, G-2 on current main (llm-config.ts, canonical.ts, zod schemas, resolver callers).
  - INV-B: evidence/filesystem/recovery — E-1, E-2, E-3, F-I5-1, F-I5-2, G-1 (state.ts journal/supersede/lockfile paths; existing schedules S1-S7).
  - INV-C: probe/trust boundary — F-L6-1, M-2, C-1, C-2, B-1, B-2 (renew.ts placement, architecture.test.ts guard, consent seam).
  - INV-D: durable boundary + misc — D-F-01, D-OBS-1..4, M-1, H-3, F-L7-1 (applyStateMutation, store keying, prior-report wording).
  - INV-E: browser hermeticity — H-1, H-2 (settle inventory across ALL browser/client tests, dist-guard stderr).
- **Wave 2 — implementation:** primary only, sequential by cohesion: (1) config lane (NF-1, NF-2, NF-4), (2) evidence lane (E-1, E-2, E-3, D-F-01), (3) probe pin (F-L6-1, M-2), (4) browser lane (H-1, H-2), (5) Info documentary items (F-L7-1, H-3 corrections in committed reports).
- **Wave 3 — fresh read-only verifiers:** V-A config/canonicalization/prototype; V-B filesystem/evidence/recovery; V-C consent/ContextBundle/protected contracts; V-D browser/tests/unsupported-value; V-E 27-item ledger completeness + new-finding sweep; V-F cross-contract/mutation/architecture. (MAO may consolidate if implementation delta is small.)
- No swarm: 5 investigators + ~5-6 verifiers + primary. Every seat has concrete justification (independent verification risk; context offloading of large lane reports).

## 5. Dependency / order graph

```
ledger/plan ──► INV wave (parallel) ──► adjudication matrix (27 rows: ACTIONABLE/BOUNDED/…)
   ──► impl 1 config (NF-1,NF-2[,NF-4]) ──► impl 2 evidence (E-1,E-2,E-3,D-F-01)
   ──► impl 3 probe pin (F-L6-1[,M-2]) ──► impl 4 browser (H-1[,H-2])
   ──► impl 5 docs Info (F-L7-1,H-3) ──► composition C1-C6 ──► mutations (disposable worktrees)
   ──► full gates + Node22/24 matrix ──► verifier wave ──► consolidation ──► graphify refresh ──► final reports ──► handoff
```

## 6. Authority domains (Phase 3 mapping inputs)

Filesystem ownership: FilesystemCapability / O_EXCL supersede-create / ownership-conditioned remove (L5). Durable state: RenewalStateTransaction, applyStateMutation reader-first boundary (I6). Identity: StructuralIdentity/ContextBundle (I2/I4). Consent: routeBinding preimage + TOTAL gate (L2). Canonicalization: null-prototype container (L1). Recovery: C==B grants / C>B never / C<B fail-closed / unreadable fail-closed (S5-H-01). No new trust primitives planned; any need ⇒ Audit Council recommendation, not unilateral creation.

## 7. Protected invariants (must remain closed)

S5-H-01, S5-M-01..04; NEW-F-01; original-14 closures (12 VERIFIED_CLOSED + L6/I3 boundaries); prior 4 verifier Low fixes (V-L1..V-L4 HELD); recovery C-matrix; consent binding; bundle identity factors; canonicalization null-proto; coverage floors 91/89/96/91.

## 8. Reproduction strategy (Phase 2)

Per residual: re-derive exact code path on main 1b7fe6e (audit cited e47c448 lines; confirm), write throwaway probe (node -e / vitest scratch in disposable worktree or /tmp) proving current behavior, classify reachability. Output: per-finding evidence rows feeding 02 reports. Investigators return findings + evidence; primary re-verifies load-bearing claims.

## 9. Mutation strategy (Phase 5)

Every actionable residual: semantic mutation in a disposable detached worktree (pattern proven by prior programs: worktree apply → targeted test run → expect failure → discard). Mutants: E-1 restore untyped escape; E-2 restore unconditional unlink; E-3 restore single-bucket EEXIST cause; F-L6-1 remove pin (and late-placement mutant must fail new test); NF-1 restore plain string keys; NF-2 restore bare bracket lookup; D-F-01 restore raw throw; H-1 restore fixed sleep (delayed-transport variant must fail). Accepted-boundary Infos: remove their guard/test and prove detection where a mechanical guard exists. Compile errors don't count.

## 10. New-finding policy

Critical/High → resolve before handoff. Medium → fix or escalate to owner. Low/Info → normally resolve in-program (final known-residual program before v0.2.1); theoretical/unreachable bounded observations may be ACCEPTED_INVARIANT_BOUNDARY only with strong evidence + mechanical bound + reopen condition. Zero silent deferrals.

## 11. Test/runtime matrix

Primary: Node dev runtime (repo default) full gates: `pnpm --filter ./packages/spec-core {build,lint,test,test:coverage,smoke:packed}` (note: run from package dir or verify filter matches — known pnpm -C/--filter false-green trap; content-verify summaries), `git diff --check`, schema freshness, frozen-spec, architecture guards, root invariants, Renewal E2E, recovery crash suites, MCP consent suites, ContextBundle identity, browser/client suites, graphify canary, NEW-F-01 concurrency regression. Secondary: Node22+Graphify0.9.50 and Node24+Graphify0.9.53 targeted (trust, recovery, config/canonicalization, browser, graphify canary) — via available node version managers; document what was actually runnable locally.

## 12. Escalation conditions (Audit Council — recommend only)

New trust primitive required; filesystem cleanup authority redefinition; L6 physical-boundary semantics change; disputable consent semantics; new canonicalization trust domain; rollback-authority change; disputed Critical/High; verifier material disagreement on trust conclusion.

## 13. Commit strategy

```
1. docs(program): canonical 27-residual ledger + plan
2. fix(config): own-key roles schema + Object.hasOwn profile resolution   [NF-1, NF-2, NF-4]
3. test(config): pin special-key authority + own-property resolution
4. fix(evidence): typed unlink/cleanup failures + fenced remove          [E-1, E-2, E-3]
5. fix(trust): typed refusal for non-serializable durable payloads        [D-F-01]
6. test(evidence+trust): cleanup/restart schedules + bigint boundary + probe pin [F-L6-1, M-2]
7. test(browser): replace fixed settle() waits with observable state      [H-1, H-2]
8+. Info closures as cohesive commits (docs corrections / guards / pins)
final: test(trust): cross-residual composition; chore(graphify): refresh; docs(audit): final reports
```
Actual grouping follows source cohesion; no per-assertion commits; no opaque mega-commits.

## 14. Freeze / prohibitions

No version bump, no v0.2.1 tag, no npm publish, no dist-tag, no GitHub Release, no push, no PR, no merge, no branch deletion. Package stays lco-spec@0.2.0. Status ceiling: READY_FOR_FINAL_LOW_INFO_TARGETED_REAUDIT / NOT_READY_…. Independent re-audit owns the verdict.

## 15. Success target

27/27 original dispositions, 0 OPEN; 0 unresolved C/H/M; 0 silently deferred actionable new Low/Info; protected findings all still closed; mutations caught; compositions green; all gates green; both Node matrices evidenced; truthful reports; handoff complete.
