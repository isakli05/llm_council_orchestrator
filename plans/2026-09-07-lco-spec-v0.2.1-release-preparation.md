# Plan — lco-spec v0.2.1 Release Candidate Preparation

Date: 2026-09-07
Operator: release-candidate preparation operator (single primary, MAO loaded, zero subagents)
Mode: release preparation — NOT a hardening program. No reopening audited runtime contracts.

## 1. Base identity (verified)

- Base SHA: `2ae42a1c3e9da96c9c3f7873e90426576ff3ccb5` (= `origin/main`, = local `main`)
- Base tree: `143c390ca0f9b89a7362b503e34cb5f4566e3f26`
- Main parents: `1b7fe6e3a4a7fc30bbbe5f23cfa1482ba901a7ea` (PR #6 merge) + `86f24b33ccee1b8bec5706fa6133b0c263ec8465` (PR #7 branch head)
- Release branch: `release/lco-spec-v0.2.1` created fresh from the exact base SHA; branch was absent locally and remotely beforehand.
- Tracked tree clean at branch point (pre-existing untracked audit artifacts left untouched).

## 2. Historical immutable release

- `v0.2.0` → `e7dedf034e92fc57616124dbdd7fe6ebffda8620` (lightweight tag, verified identical local + remote; GPG-signed merge commit via GitHub)
- `v0.2.0` MUST remain untouched for the whole program.

## 3. Registry / tag preflight (verified 2026-09-07)

- `npm view lco-spec version` → `0.2.0`; dist-tags → `{ "latest": "0.2.0" }`
- `npm view lco-spec@0.2.1 version` → E404 (unpublished) = success
- Local tag `v0.2.1`: absent. Remote tag `v0.2.1`: absent.

## 4. Release convention (reconstructed from Git, not memory)

- v0.2.0 preparation commit: `f8e74ae` on `release/lco-spec-v0.2.0` —
  `chore(release): prepare lco-spec v0.2.0`, changing ONLY
  `packages/spec-core/package.json` (`0.1.0` → `0.2.0`). No CHANGELOG file was
  created (none existed; no gate requires one — prior release-prep report 01
  documents this decision).
- Changelog surface: `packages/spec-core/README.md` § "Değişiklik Günlüğü" is the
  repository's established, dated release-history section. Its own discipline
  note requires a dated entry per merged stage/version; in practice entries
  lapsed after 2026-08-30 during the audit era and v0.2.0 itself shipped
  without one. For v0.2.1 this program ADDS a dated v0.2.1 entry there
  (program mandate: release notes go to the established surface; the section is
  that surface; a new CHANGELOG.md is NOT created).
- Lockfile: `pnpm-lock.yaml` carries no self-version field for the workspace
  package → no lockfile change required for a version bump.
- Root `package.json` (0.1.0, `private: true`): workspace metadata only; nothing
  in publish/CI/runtime reads it → unchanged.
- Publish model (README § "Yayın ve Sahiplik" + `.github/workflows/publish.yml`):
  bump + changelog → full gate → commit → exact `vX.Y.Z` tag → dispatch
  `publish-spec-core` from the tag, `dry_run` default true; real publish is a
  separate owner-authorized OIDC Trusted Publishing step. NONE of that runs in
  this program.

## 5. Version-surface ledger (census @ base)

| # | Surface | Class | Action |
|---|---------|-------|--------|
| 1 | `packages/spec-core/package.json` `version` | A — live package identity | **0.2.0 → 0.2.1** |
| 2 | README § Değişiklik Günlüğü | D — current release docs | **add dated v0.2.1 entry** |
| 3 | `packages/spec-core/src/release/version.ts` (readPackageVersion) | B — runtime identity source | no edit (derives from package.json at runtime) |
| 4 | `lco --version` (`src/cli/index.ts`) | B | no edit (prints readPackageVersion()) |
| 5 | `lco-mcp` `serverInfo.version` (`src/mcp/server.ts` SERVER_VERSION) | B | no edit (readPackageVersion()) |
| 6 | `src/release/readiness.test.ts` `'v0.2.0'` / `'0.1.0'` literals | E — abstract tag/version-mismatch fixtures | keep |
| 7 | `src/release/version.test.ts`, `src/mcp/server.test.ts`, `scripts/packed-install-smoke.sh` comments "(v0.2.0 blocker)" | E — historical references | keep |
| 8 | `pnpm-lock.yaml` `eastasianwidth@0.2.0` | G — unrelated external dep | keep |
| 9 | README L875/L1385/L1415 `"0.1.0"` / `v0.1.0` | G — historical/doc examples | keep |
| 10 | root `package.json` 0.1.0 | workspace metadata (private, unread) | keep |
| 11 | `audit-output/**`, `plans/**` historical reports mentioning v0.2.0/lco-spec@0.2.0 | E — historical immutable evidence | keep |
| 12 | `dist/**`, `generated/**` | derived build output | rebuilt by gates; Phase 7 grep proof |

Runtime version authority is single-sourced (`readPackageVersion()` reading the
packed `package.json`; identity guard on `name === 'lco-spec'`). The bump
therefore requires ZERO runtime-logic changes → no
VERSION_BUMP_REQUIRES_RUNTIME_LOGIC_CHANGE condition.

## 6. Allowed change scope (the whole release delta)

1. `packages/spec-core/package.json`: version `0.2.0` → `0.2.1`
2. `packages/spec-core/README.md`: one dated v0.2.1 entry in Değişiklik Günlüğü
3. This plan file.

Nothing else. No trust/recovery/consent/ContextBundle/filesystem/canonical/
clarification/LLM-routing behavior. Unrelated bugs discovered → recorded, not
fixed.

## 7. SemVer justification — 0.2.0 → 0.2.1 (patch)

`git diff v0.2.0..2ae42a1` (46 commits, PRs #5/#6/#7):
- `src/schemas/` + `generated/`: ZERO diff → no schema/API break.
- `src/cli/index.ts`: unchanged → no CLI grammar/flag break.
- `src/mcp/server.ts`: internal consent-state hardening (L2 fail-closed route
  binding); tool list, method surface, protocolVersion, serverInfo shape
  unchanged → no MCP protocol break.
- `engines.node >=22`: unchanged.
- Content = defensive fixes (trust kernel, canonicalization, ContextBundle
  identity, paid-route headers, abort-evidence persistence, durable-store
  write boundary, config profile/roles resolution), tests, docs truth
  corrections → bugfix/hardening = patch.

## 8. Local gate matrix (at candidate)

```
pnpm --filter ./packages/spec-core build
pnpm --filter ./packages/spec-core lint
pnpm --filter ./packages/spec-core test
pnpm --filter ./packages/spec-core test:coverage
pnpm --filter ./packages/spec-core smoke:packed
git diff --check
```

Expected neighborhood: 194 files / 2708 tests / 0 skipped; coverage ratchets
statements ≥91 / branches ≥89 / functions ≥96 / lines ≥91 (never lowered).
Node/Graphify local evidence recorded as actually executed (no dependency
changes merely to fake matrix parity; remote CI owns the authoritative matrix).

## 9. Pack + fresh-install proof (NO publish)

- `npm pack --dry-run --json` from `packages/spec-core` → record name/version/
  file count/sizes/shasum.
- Real pack into a disposable dir OUTSIDE the repo; record local SHA256.
- List tarball contents; prove absence of `.git`, `audit-output`, `plans`,
  tests, `.env`/secrets/credentials/logs.
- Fresh-install the EXACT tgz in a clean temp consumer dir; prove version
  0.2.1, bins `lco` + `lco-mcp`, CLI `--version`, MCP initialize handshake
  `serverInfo.version = 0.2.1`, established packed smoke surface. Node22 +
  Node24 where practical.
- Delta vs published `lco-spec@0.2.0` tarball: category-level explanation of
  every add/remove/change (byte identity NOT expected — hardening intentionally
  changed dist output).

## 10. Readiness behavior (pre-tag)

`prepublishOnly` → `scripts/prepublish-check.js` → `src/release/readiness.ts`.
At the clean release-candidate commit with NO v0.2.1 tag: expected result
`EXPECTED_PRETAG_REFUSAL` — refusal whose ONLY blocking reason is the
deliberately absent exact tag. Any additional blocker (dirty tree, version/tag
mismatch unrelated to absence, repo mismatch) → investigate, never bypass.

## 11. PR / remote CI plan

- Push `release/lco-spec-v0.2.1` normally (no force); remote branch SHA must
  equal the exact candidate SHA.
- ONE PR `release/lco-spec-v0.2.1` → `main` (discover first; no duplicates).
  Title: "Prepare lco-spec v0.2.1 release". Body carries exact base/candidate
  identities, SemVer justification, gate + tarball + fresh-install proof, and
  the NO-TAG/NO-PUBLISH banner.
- Authoritative remote CI (checked-in `ci.yml`): Node22+Graphify 0.9.50 AND
  Node24+Graphify 0.9.53 on the exact PR head; collect run/job IDs, versions,
  test/coverage numbers, packed-smoke version evidence (must report 0.2.1).
- Merge-ref: second parent == exact candidate HEAD; first parent == 2ae42a1
  (base unchanged); conflict-free normal merge ⇒ merge-ref tree == candidate
  tree (empty tree diff).

## 12. Prohibitions (absolute, whole program)

NO merge · NO v0.2.1 tag create/push · NO tag moves · NO npm publish (real or
`--dry-run` on this branch) · NO publish workflow dispatch · NO dist-tag
changes · NO GitHub Release · NO release-branch deletion · STOP before merge
with `READY_FOR_OWNER_RELEASE_PR_MERGE_AUTHORIZATION`.

## 13. Reports

Untracked `audit-output/lco-spec-v0.2.1-release-preparation-2026-09-07/`
(00-STATUS … 10-PR-INTEGRATION-HANDOFF). Report writing must never mutate the
candidate.
